# 关于选修课自动抢课

> 定位修正：这个项目不只是"抢课工具"，更是**学「网络 / HTTP / 自动化 / 逆向」的载体**。
> 准则：**自用 + 低调 + 先查校规**，别公开、别商用。

---

## 📋 源头信息（灵感模板）

- **源头场景**：学校软件网页不稳定 + 人多时抢不到课
- **目标用户**：内江师范学院同学
- **核心需求**：及时抢到指定课程
- **初步解决思路**：UI 让用户提前选课 → JSON 存选择 → 系统开放后自动登录并确认选择
- **技术初判**：爬取选课界面并选择有点难；AI 做 UI 很舒服
- **状态**：待孵化（定位为"学网络"项目）

---

## 🔍 学校教务系统调研（2026-09）

- 教务入口：`http://jxglpt.njtc.edu.cn/sso/driotlogin`
- **有统一身份认证（SSO）**；**外网需 VPN** 才能访问教务系统
- 大概率**非标准"正方/青果/强智"**，GitHub 上对口的开源项目需**自行适配**（SSO + VPN + 可能验证码）

---

## 🎯 MVP 拆解（最小可行产品）

- **MVP0 · 能登录+会话**：用 `requests` + `Session` 模拟登录（SSO），拿到 cookie，能访问选课页
- **MVP1 · 能提交一次选课**：抓包找选课接口，用 `session` 成功提交一次选课请求
- **MVP2 · 定时自动开抢**：开放时刻自动触发 + 简单重试
- **非 MVP（砍掉**）：UI、并发、课程筛选、可视化控制台、换课

> 一句话：**登录 + 会话 + 提交一次选课 = 最小可行**。

---

## 📚 知识点

| 级别 | 知识点 |
|---|---|
| 🔴 必备 | HTTP（GET/POST/状态码）、**Cookie/Session**、`requests`（Session 保持登录态）、**抓包（F12→Network）** |
| 🟡 建议 | 登录/认证（SSO/CSRF）、JSON（已会）、异常/重试 |
| 🟢 进阶 | 定时/调度、并发、浏览器自动化（Playwright）、防反爬 |
| 🎯 已有底子 | HTTP POST（urllib+JSON+Bearer）、API/Key、`session`、JSON（agent 里学的） |

---

## 🛠️ 学习路径（开学后）

1. **抓包**：F12 → Network，登录/选课各操作一次，记录 URL、method、POST 数据、cookie
2. **模拟登录**：`requests.Session()` 发登录请求，拿会话，验证能否访问选课页
3. **模拟选课**：从抓包拿参数，用 `session` 提交一次选课请求（先试一次，别真抢）
4. **定时开抢**：开放时刻自动触发 + `while`/`sleep` 重试（正好用上会了的 while）
5. **进阶**：并发、浏览器自动化、验证码（按需）

---

## ⚠️ 合规提醒

- **真拿去抢课 → 先查学校规矩**；**自用 + 低调 + 别公开/商用**（否则是"外挂/黄牛"性质，风险大）
- **学到技术是真本事，抢到课只是运气**
