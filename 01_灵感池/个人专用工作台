# 个人专用工作台

- **源头场景**：每天处理很多学习任务，和待办的其他事务，过于零散，不方便我学习。
- **目标用户**：我自己（单用户私有工具，不是多人平台）。
- **核心需求**：方便看见我的每日待办学习任务或工作/学习规划。

## 初步解决思路
构建一个 UI 界面 → 用 JSON 保存每日待办和规划 → 一个「打开即用」的本地面板（**不是网站平台**）。
核心数据流向：用户输入 → 本地存储（localStorage / JSON 文件）→ 「今日」视图 + 「全部」列表展示。

## 技术初判（已修订）
- **UI 构建不难**：有 AI + 组件库（Tailwind / shadcn 这类）很容易做出干净界面；真正难的是「功能 / 范围设计」。
- **「搭建个人网站平台最省事」是误区**：它需要部署、域名、鉴权，对单人工具是巨大的外围负担。
- **关键定位修正**：一个只给一个人用、打开就能用、本地化的待办 / 学习规划面板。做成「工具」，不是「平台」。

## 状态：待孵化

## 最小可行产品（MVP）范围
- 添加任务：标题 + 可选截止日期 + 可选标签/分类
- 「今日」视图 + 全部任务列表
- 标记完成 / 编辑 / 删除
- 自动保存
- 可选：优先级（高 / 中 / 低）

## 暂不实现（v1 明确不做，避免范围膨胀）
- 账号登录、多设备同步
- 日历视图、提醒推送、打卡统计
- AI 自动归类
- 移动端 / 桌面端 App

## v2 可考虑
- 学习计划 / 周视图，习惯打卡与连击统计
- 标签筛选、日历视图
- 跨设备同步
- AI 帮你把任务自动分到今天

## ⚠️ 需要警惕的潜在风险
**范围膨胀是头号敌人**
一旦想着「先搭平台、做登录、做同步」，就会像 USB 项目一样拖到「已放弃」。v1 永远只做本地单页。

**localStorage 数据会丢**
清浏览器缓存就没了，且数据绑死在这台电脑 + 这个浏览器。所以必须有「导出 / 导入 JSON」的备份功能（做起来很简单，还能顺便满足你「用 JSON 存数据」的初衷）。

**别做成「平台」**
「平台」是给很多人用的，你是只给自己用——做成「打开即用的工具」，不是「平台」。
