# LeetCode 刷题记录

> **刷题心法（每道题走完这个循环才算刷完）：**
> 1. 独立思考 15–30 分钟，不看题解
> 2. 卡住 → 看题解，看懂后**关掉自己重写一遍**
> 3. 写完再想：能否更简单？时间/空间花在哪？
> 4. 填一行记录，隔几天**重做一遍**（间隔复习，对抗遗忘）
>
> **复习标记：** ✅ 能独立 AC ｜ 🔄 需要复习 ｜ ❌ 还没吃透
> **进度统计：** 暑假 16 道 + 大学期间新增

| 日期 | 题号 | 题目 | 难度 | 思路（一句话） | 卡点 | 收获 | 复习 |
|---|---|---|---|---|---|---|---|
| 2026-08-08 | 1 | 两数之和 | 简单 | 双层循环找和为 target 的两个下标；进阶用哈希表 O(n) 一遍 | 返回写成 `nums[i, j]`（元组索引）抛 TypeError | 返回多个下标用列表 `[i, j]`，不是 `arr[a, b]`；循环后补 `return []` 兜底 | 🔄 |
| 2026-08-08 | 509 | 斐波那契数 | 简单 | 递推 `F(n)=F(n-1)+F(n-2)`；可用递归或自底向上迭代 | 不理解递归：不知道"递推/回溯"流程，递归写法处理不来 | 递归三要素：**出口（基线 F(0)/F(1)） + 递推关系**；大 n 用**迭代**自底向上更省栈，别用递归硬扛 | ✅ |
| 2026-08-10 | 14 | 最长公共前缀 | 简单 | 以第一个字符串为基准，逐字符横向比较所有字符串 | 暴力/嵌套循环没写出来 | 横向比较：拿第一个串当基准，逐位对比其余串，遇不匹配或到任意串末尾就停；**空数组直接返回 ""** | 🔄 |
| 2026-08-11 | 27 | 移除元素 | 简单 | 快慢双指针：快指针找非 val，慢指针占位并原地覆盖 | 用 `for i in nums` 直接遍历，把元素值当索引用；慢指针初始值从 1 开始，漏掉可能是目标的第一个元素 | 慢指针**从 0 开始**，因为第一个元素也可能要被删除 | 🔄 |
| 2026-08-11 | 26 | 删除有序数组中的重复项 | 简单 | 快慢双指针：慢指针记录唯一元素位置，快指针跳过重复 | 写完了 `nums[k] = nums[i]` 但忘记 `k += 1`，慢指针没动，所有元素被覆盖到同一位置 | 快指针探路、慢指针接收，**每次接收后慢指针必须自增** | 🔄 |
| 2026-08-12 | 1480 | 一维数组的动态和 | 简单 | 前缀和：从下标 1 起 `nums[i] += nums[i-1]` | 循环从 0 开始，i=0 时 `nums[-1]` 取到最后一个元素（负索引陷阱） | 用到 `nums[i-1]` 时 range 必须从 1 开始；负索引表示倒数第几个 | 🔄 |
| 2026-08-12 | 1768 | 交替合并字符串 | 简单 | 双指针 i、j 交替取字符，长的一串把多出部分追加到尾部 | ① 字符串索引用小括号 `word1(i)` 被当函数调用；② 变量名 `word`→`word2` 写错；③ 末尾多余冒号 | Python 取下标一律用 `[ ]`，`( )` 是函数调用 | 🔄 |
| 2026-08-13 | 389 | 找不同 | 简单 | ASCII 累差：t 字符码加总减 s 字符码加总，差值即多出的字符 | 曾用 `set` 去重，丢失重复次数（s="a",t="aa" 时无法区分） | 计数/`ord`/`chr` 累差才能保留重复信息；集合只处理"有无" | 🔄 |
| 2026-08-13 | 28 | 找出字符串中第一个匹配项的下标 | 简单 | 滑动窗口/切片比较，循环范围 `range(n-m+1)` 覆盖所有可能起点 | 不理解 `range(n-m+1)` 为何要 +1 | range 包头不包尾，为包含最后起点 n-m 需写成 n-m+1 | 🔄 |
| 2026-08-13 | 242 | 有效的字母异位词 | 简单 | 异位词=字符频率相同，用 `Counter(s)==Counter(t)` 或 `sorted` 比较 | 曾按相同位置逐位比较，忽略顺序可打乱；j 未正确更新 | 不能用 ASCII 累加判断（"ac" 与 "bb" 累加值相同）；计数/排序是标准做法 | 🔄 |
| 2026-08-13 | 459 | 重复的子字符串 | 简单 | 枚举能整除 n 的子串长度 i，用 `sub*(n//i)==s` 判断是否由 sub 拼接 | ① 误解 `n%i!=0` 为"小数"，实为判断能否整除；② 不理解 `sub*(n//i)` 字符串乘法 | `sub*count` 表示重复拼接；候选长度只需枚举到 n//2；长度可为 1 | 🔄 |
| 2026-08-14 | 2 | 两数相加 | 中等 | 链表转数字相加，递归/迭代处理进位，长度不等用 None 补齐 | ① 不理解递归"递推/回溯"流程，`current_node.next = add(...)` 在干嘛；② 把链表当列表用 `for i in l1` | 递归出口：`if not a and not b and carry==0`；链表只能用 `node.next` 顺藤摸瓜访问，**没有下标和 len**；`extend(+=)` 右侧必须可迭代对象 | 🔄 |
| 2026-08-18 | 3471 | 找出最大的几近缺失整数 | 简单 | 对每个子数组先去重再统计频次，取"恰好在 1 个子数组中出现"的最大数 | 用 `freq[x] += 1` 直接统计所有子数组，导致同一个 x 在同一子数组重复计数，count==1 筛选失效（如 `[0,0]` 本应输出 0 却输出 -1） | 先对每个子数组 `set(sub)` 去重再累加；题目问的是"出现在**几个**子数组中"，不是"出现**几次**" | 🔄 |
| 2026-08-20 | 1386 | 安排电影院座位 | 中等 | 按行统计可坐家庭数：空行 +2，被占的用集合交集判断哪些块可用 | ① 试图用 `delete` 删除预订座位（方法名错）；② 对"空行直接加 2"不理解，以为每行最多 1 组 | 用**集合交集**判断块是否被占；完全空的行可同时坐 [2,3,4,5] 和 [6,7,8,9] 两组，**空行要加 2** | 🔄 |
| 2026-08-24 | 283 | 移动零 | 简单 | 快慢双指针：快指针找非零，慢指针占位并交换到前面 | ① while 手动管下标 + pop/append 死循环（超时）；**重做时**：② 交换误用 `==`（比较）而非 `=`（赋值）；③ `range(1,...)` 跳过下标0，若 `nums[0]` 非零会出错；④ for 循环里手动 `right += 1` 多余 | **双指针是万能钥匙**：慢指针放"要留的元素"（非零）+ 快指针找值；**非零才搬到前面，零跳过自然掉队**。**`for` 循环自动前进、不能手动改循环变量**；交换用 `=`；循环起点看第 0 个元素要不要处理。重做复盘后理解更深 | 🔄 |
| 2026-08-25 | 66 | 加一 | 简单 | 从末位往前找第一个 `<9` 的位 +1 后返回；遇 9 就置 0 往前进位；若全为 9 则最前面补 1 | ① 索引写成 `digits[a]`（a=len）越界；② 把"数字 9"误当成"数组长度=9"，进位逻辑完全走偏；③ `remove(a)` 删的是值不是位置、`extend(1,0)` 语法错 | 加一的本质是**从右往左找第一个 `<9` 的合法下标**，而不是看长度；索引范围 0~len-1；Python `extend` 要传可迭代对象 | 🔄 |
| 2026-08-26 | 1822 | 数组元素积的符号 | 简单 | 遇 0 直接返回 0；否则统计负数个数，偶数→返回 1，奇数→返回 -1 | ① 不会用代码表达"负数"（不知是 `num < 0`）；② `range(nums)` 把列表当成 range 参数，`if i = 0` 用 `=` 赋值而非 `==` 比较 | 判断负数用 `num < 0`；乘积符号只需看**有没有 0** 和**负数个数的奇偶**，不用真算乘积（避免大数溢出） | 🔄 |
| 2026-08-27 | 1502 | 能否形成等差数列 | 简单 | 先排序，再检查所有相邻差是否恒定 | ① 漏了排序（题目允许重排，必须 `sort()`）；② `while i in range(...)` 循环写法错；③ 在 if 里第一次就 `return True`、且 `arr[i+1]` 越界 | **"可重排"的题先排序**；排序后只需检查相邻差恒定；`len<=2` 自动为 True，无需特判。**间隔复习重做，5 分钟 AC** → 套路已吃透 | ✅ |
| 2026-08-28 | 896 | 单调数列 | 简单 | 用 `inc`/`dec` 双标志，或用 `any([all(递增), all(递减)])` 表达 | 第一版想"逐个判断全部满足递增/递减"，但"满足一个就跳出循环"卡住，不知怎么表达 | **正难则反**：验证"全部单调"困难，就反过来——**只要有 1 个违反就否定**。**核心思路自己想到**；学会了 `all()`、`any()`、生成器表达式；已能**从零独立写出**（all/any 版），掌握"两种单调性任一成立" | ✅ |
| 2026-08-29 | 13 | 罗马数字转整数 | 简单 | 字典映射字符→数值；当前值 `<` 右值则**减**，否则**加** | ① 一开始没想到用字典做映射；② 看不懂 `roman_map[s[i]]` 的嵌套（其实是 `f(g(x))` 由内到外）；③ 把变量名 `roman_map` 和内置函数 `map()` 混淆 | **减法规则只在小数在大数左边时发生**；字典是"查表"不是"批量加工(map)"；嵌套 = 先内后外（类比 `f(g(x))`）。此版为**引导式学习**，非独立完成 | 🔄 |
| 2026-09-01 | 58 | 最后一个单词的长度 | 简单 | 从末尾跳过空格，再从后往前数非空格字符 | ① `range(s)` 把字符串当 range 参数（应是 `range(len(s))`）；② 第一个 while 条件写错（`i==0` 无法跳过末尾多个空格）；③ 把 `while` 当"退出条件"理解、`i`(位置)与 `length`(计数)搞混 | **`while` 后面是"继续条件"**：为 True 就继续、False 就停；从后往前用 `i-=1`；`i`(位置)和 `length`(计数)各自独立。**彻底理解 while 循环是本轮最大的收获**（此前一直不知道 while 的运行机制） | 🔄 |
| 2026-09-02 | 709 | 转换成小写字母 | 简单 | 用 `ord()` 取字符 ASCII 码，若在 65-90（大写）则 `+32` 转小写，再 `chr()` 转回并拼接 | ① `for` 没缩进进 `def`；② `if s[i] >` 缺少比较对象；③ 没生成新字符串直接返回原串；④ 想用 ASCII 判断大小写，但不知道要 `ord()`/`chr()` | 大写 A-Z = 65-90，小写 a-z = 97-122，**相差 32**；`ord()` 拿码、`+32` 转小写、`chr()` 转回字符。**"懂思路但写不出"= 缺 `ord`/`chr`/`+32` 这几个"零件"** | 🔄 |
|  |  |  |  |  |  |  |  |

## 同类题串（做一道，串一串）

- **双指针·原地操作**：27 移除元素 → 26 删除有序数组中的重复项 → 80 删除有序数组中的重复项 II → 283 移动零
- **链表双指针**（大二遇到）：141 环形链表、876 链表的中间结点
- **滑动窗口**（进阶）：3 无重复字符的最长子串

## 已刷清单（暑假 16 道，已补录完毕）

> 上面表格已收录暑假 16 道 + 大学期间新增，**卡点/收获全部填实**。复习时用上面的「★ 复盘教训」区按类型扫一遍即可。

## ★ 复盘教训（按类型 · 查漏用）

### 一、数组双指针 / 原地修改
- **原地修改数组，优先用双指针**，别改列表长度。
- 遍历若涉及 `i-1`，**循环起点必须是 1**，否则 `nums[-1]` 取到末尾元素。共同题：26 / 27 / 283 / 1480。

### 二、字符串与哈希统计
- 涉及"出现次数 / 字符组成"→ 优先 **Counter 或排序**；涉及"重复构成"→ 必须检查**长度整除性**。共同题：389 / 242 / 459。

### 三、链表与递归
- 链表不是数组，只能用 `node.next` 顺藤摸瓜，**没有下标和 len**。
- 递归关键：想清楚**出口**和**如何拼接返回值**。共同题：2 两数相加。

### 四、子数组 / 频次统计
- 先明确统计口径：是"出现**次数**"还是"出现在**几个**子数组"？（如 3471）
- 座位/块类注意**块与块是否重叠**、空行/空块带来的额外收益（如 1386）。

## 复习计划（可选）

- 每刷 5 道新题，回头重做 1 道旧题
- 每周日花 30 分钟：随机抽 3 道旧题，限时重写

2026-8-24，我学会了git push
