这个文件主要用于更新我的学习进度

---

# 📅 周学习总结（2026-08-25 ~ 08-28）

## 🔑 本周新增核心技能

**Python**
- 递归：从"处理不来"→ 独立写出 fib(10)=55，懂"出口 + 递推关系"、递归 vs 迭代
- `==` vs `=`、索引越界、`range` 用法、负索引陷阱（`nums[-1]`）
- 面向对象（类）：`class`、`__init__`、方法、`self`
- 新工具：`all()`、`any()`、生成器表达式、位运算（`& 1`）
- 复杂度：O(n) vs O(n log n)

**JavaScript + 浏览器扩展**
- DOM、`querySelectorAll`、`el.style.display='none'`（藏≠删）
- `manifest.json`、content script vs background、`chrome.storage`
- 做出最小扩展 `my-first-ext`（隐藏全页图片）

**算法思维**
- 正难则反（896 单调数列，自己想到的）
- "可重排的题先排序"（1502）
- 乘积只看符号不算数值（1822，避免溢出）

**物理建模（电梯仿真）**
- 电机功率 P=Fv、阻力（重力+摩擦+风阻）、净力→加速度→速度
- 负载自适应：乘客多→阻力大→减速

**Agent 基础（新项目）**
- 什么是 agent（LLM + 循环 + 工具）
- 装好 Ollama + `qwen3:4b` 本地模型
- `messages` 记录本、`system` 角色、`ollama.chat` 调用格式

## 📁 本周完成
- 16 道 LeetCode 全记录、卡点/收获填实；本周新增 66、1822、1502、896
- 破解递归 + 独立 AC 单调数列(896)
- `my-first-ext` 最小扩展能跑
- 电梯仿真项目重组（`elevator_model.py` 等）+ 读懂物理引擎
- CS50x / CS速成课 笔记模板 + Week 0 进度（课看 2 遍）
- 正在搭自己的 agent（`agent_v1`）

## 🏆 最值钱的成长：学习方法升级
- 从"让 AI 代劳"→"我写、AI 当教练"
- 几次抓出 AI 直接给答案，说明"不能靠 AI 代劳"标准已内化
- 诚实区分"我的思路" vs "AI 的代码"
- 懂了"会"的四层（看懂→默写→变体→讲解）

> 一句话：**从"跟着学"变成"主动学"，从"靠 AI 做"变成"让 AI 陪我学"。**

---

# 📅 周学习总结（2026-09-01 ~ 09-04）

## 🔢 LeetCode（本周 4 道，1491 AC）
- 58 最后一个单词的长度：**while 循环搞懂**（继续条件 ≠ 退出条件）
- 709 转小写：`ord()`/`chr()`、ASCII +32
- 682 棒球比赛：**用列表记历史**（C/D/+ 需要），`pop()`
- 1491 平均值：**`sum-min-max` 减法视角**、切片 `[1:-1]`、AC ✅

## 🧠 核心概念突破
- `while` = 继续条件（绿灯亮就继续）
- `for` = 每个值一遍、自动前进、别手动改
- 列表 = "记忆/栈"（需要历史就用列表）
- `sum - min - max`（从总数减被排除的，不真删）
- 切片 `[1:-1]`、负下标
- 方法调用要加 `()`（`sort()`）

## 📐 高数预习（Day 1-2 ✅）
- Day 1 函数：复合、反函数（先交换 x/y 再解出 y）
- Day 2 极限：趋近、0/0 的"洞"、极限=无限过程的归宿（0.999...=1）

## 🏆 从仓库挖出的"意想不到的所学"
**你独立写出并反复练熟了一个真正的 AI agent！**（本周最大的隐藏成就）
- Python + DeepSeek API 写出 `agent.py`/`mini_agent.py`，含**工具调用 tool_calls + 回填**，**独立写 4 遍到全对**
- 深入理解：`call_llm`、`tool_calls`、`tool_call_id`、`msg vs messages`、`.get()`、`continue vs return`
- 学到：**HTTP POST（urllib+JSON+Bearer）、API/API Key、HTTP vs HTTPS**（网络与安全常识）
- 系统沉淀**"怎么用 dsh 学"方法论**：教练角色、四种用法、防滑规矩、复述、画结构
- **Git**：做了命令速查/输出翻译手册；**dsh 机制**：上下文窗口/压缩/省钱时间表

> 一句话：**这周你不光刷题、开了高数的门，还亲手搭出并真正理解了 agent（大脑+循环+工具），并把"怎么学"变成了一套方法。收获远超你嘴上说的。**
